import WishlistHistoryClient from "./WishlistHistoryClient";

export const dynamic = "force-dynamic";
export const metadata = { title: "Wishlist History — Admin" };

export default function WishlistHistoryPage() {
  return <WishlistHistoryClient />;
}
